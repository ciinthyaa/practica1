Accciones a tener en cuenta:

## En el caso de no tener las librerias instaladas hacer uso del siguiente comando: 
install.packages("nombre de la libreria") 

Para que cada punto de el código funcione es necesario que se importen los datos, en este caso:
## Se debe modificar la ruta en la cual estará el archivo con los datos, en nuestro caso la ruta era la siguiente:  "~/Ciberseguridad/5 DataDriven/PRACTICAS/epa-http/epa-http.csv" por ello el código apunta a esta.
